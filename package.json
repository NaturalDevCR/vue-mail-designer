{
  "name": "vue-mailcraft",
  "version": "2.0.0",
  "license": "MIT",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "pnpm --filter demo dev",
    "build": "pnpm --filter vue-mailcraft build && pnpm --filter demo build",
    "test": "pnpm --filter vue-mailcraft test",
    "typecheck": "pnpm -r typecheck",
    "check": "pnpm typecheck && pnpm test"
  },
  "pnpm": {
    "onlyBuiltDependencies": ["esbuild", "vue-demi"]
  },
  "engines": { "node": ">=20" }
}
